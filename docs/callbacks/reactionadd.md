# reactionAdd
desc
### Использование
```php
bot.onreactionAdd()
bot.reactionAddCommand
```
## Пример(ы)

```javascript
bot.reactionAddcommand({
  name: 'reactionAdd',
  code: `
`
// Возвращает: ...
})
```