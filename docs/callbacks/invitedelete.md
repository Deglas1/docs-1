# inviteDelete
desc
### Использование
```php
bot.oninviteDelete()
bot.inviteDeleteCommand
```
## Пример(ы)

```javascript
bot.inviteDeletecommand({
  name: 'inviteDelete',
  code: `
`
// Возвращает: ...
})
```