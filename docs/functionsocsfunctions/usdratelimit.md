# $rateLimit
desc
### Использование
```php
$rateLimit[(x) => x ==]
```

### Опции

| Опция | Описание | Тип | Обязательно |
|--------|-------------|------|----------|
| (x) => x == |  |  | Да |  
## Пример(ы)

```javascript
bot.command({
  name: '$rateLimit',
  code: `
$rateLimit[(x) => x ==]`
// Возвращает: ...
})
```