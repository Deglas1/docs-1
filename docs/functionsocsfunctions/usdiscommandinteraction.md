# $isCommandInteraction
desc
### Использование
```php
$isCommandInteractionnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$isCommandInteraction',
  code: `
$isCommandInteractionnull`
// Возвращает: ...
})
```