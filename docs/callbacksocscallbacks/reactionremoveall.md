# reactionRemoveAll
desc
### Использование
```php
bot.onreactionRemoveAll()
bot.reactionRemoveAllCommand
```
## Пример(ы)

```javascript
bot.reactionRemoveAllcommand({
  name: 'reactionRemoveAll',
  code: `
`
// Возвращает: ...
})
```