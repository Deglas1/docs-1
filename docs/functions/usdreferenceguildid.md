# $referenceGuildId
desc
### Использование
```php
$referenceGuildIdnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$referenceGuildId',
  code: `
$referenceGuildIdnull`
// Возвращает: ...
})
```