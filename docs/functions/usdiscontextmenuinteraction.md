# $isContextMenuInteraction
desc
### Использование
```php
$isContextMenuInteractionnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$isContextMenuInteraction',
  code: `
$isContextMenuInteractionnull`
// Возвращает: ...
})
```