# $allMembersCount
Возвращает общее количество участников со всех серверов, где находится бот
### Использование
```php
$allMembersCount
```

## Пример(ы)

```javascript
bot.command({
  name: '$allMembersCount',
  code: `
$allMembersCount`
// Возвращает: 690
})
```