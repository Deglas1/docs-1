# $mentionedChannelsCount
desc
### Использование
```php
$mentionedChannelsCountnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$mentionedChannelsCount',
  code: `
$mentionedChannelsCountnull`
// Возвращает: ...
})
```