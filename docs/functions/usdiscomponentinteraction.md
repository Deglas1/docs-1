# $isComponentInteraction
desc
### Использование
```php
$isComponentInteractionnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$isComponentInteraction',
  code: `
$isComponentInteractionnull`
// Возвращает: ...
})
```