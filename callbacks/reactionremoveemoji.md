# reactionRemoveEmoji
desc
### Использование
```php
bot.onreactionRemoveEmoji()
bot.reactionRemoveEmojiCommand
```
## Пример(ы)

```javascript
bot.reactionRemoveEmojicommand({
  name: 'reactionRemoveEmoji',
  code: `
`
// Возвращает: ...
})
```