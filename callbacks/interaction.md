# interaction
desc
### Использование
```php
bot.oninteraction()
bot.interactionCommand
```
## Пример(ы)

```javascript
bot.interactioncommand({
  name: 'interaction',
  code: `
`
// Возвращает: ...
})
```