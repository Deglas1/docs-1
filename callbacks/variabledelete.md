# variableDelete
desc
### Использование
```php
bot.onvariableDelete()
bot.variableDeleteCommand
```
## Пример(ы)

```javascript
bot.variableDeletecommand({
  name: 'variableDelete',
  code: `
`
// Возвращает: ...
})
```