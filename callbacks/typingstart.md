# typingStart
desc
### Использование
```php
bot.ontypingStart()
bot.typingStartCommand
```
## Пример(ы)

```javascript
bot.typingStartcommand({
  name: 'typingStart',
  code: `
`
// Возвращает: ...
})
```