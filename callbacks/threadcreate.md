# threadCreate
desc
### Использование
```php
bot.onthreadCreate()
bot.threadCreateCommand
```
## Пример(ы)

```javascript
bot.threadCreatecommand({
  name: 'threadCreate',
  code: `
`
// Возвращает: ...
})
```