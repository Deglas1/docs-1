# $respawnAllShards
desc
### Использование
```php
$respawnAllShardsnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$respawnAllShards',
  code: `
$respawnAllShardsnull`
// Возвращает: ...
})
```