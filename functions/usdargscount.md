# $argsCount
Возвращает количсетво аргументов в сообщении
### Использование
```php
$argsCount
```

## Пример(ы)

```javascript
bot.command({
  name: '$argsCount',
  code: `
$argsCount`
// Возвращает: 2
})
```