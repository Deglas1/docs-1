# $interactionData
desc
### Использование
```php
$interactionDatanull
```

## Пример(ы)

```javascript
bot.command({
  name: '$interactionData',
  code: `
$interactionDatanull`
// Возвращает: ...
})
```