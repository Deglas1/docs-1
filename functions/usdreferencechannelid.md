# $referenceChannelId
desc
### Использование
```php
$referenceChannelIdnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$referenceChannelId',
  code: `
$referenceChannelIdnull`
// Возвращает: ...
})
```