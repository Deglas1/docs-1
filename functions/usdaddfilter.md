# $addFilter
desc
### Использование
```php
$addFilter[filter]
```

### Опции

| Опция | Описание | Тип | Обязательно |
|--------|-------------|------|----------|
| filter |  |  | Да |  
## Пример(ы)

```javascript
bot.command({
  name: '$addFilter',
  code: `
$addFilter[filter]`
// Возвращает: ...
})
```