# $authorID
Возвращает айди пользователя, который вызвал функцию
### Использование
```php
$authorID
```

## Пример(ы)

```javascript
bot.command({
  name: '$authorID',
  code: `
$authorID`
// Возвращает: 828198636549832737
})
```