# $getCurrentDuration
desc
### Использование
```php
$getCurrentDurationnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$getCurrentDuration',
  code: `
$getCurrentDurationnull`
// Возвращает: ...
})
```