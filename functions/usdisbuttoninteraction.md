# $isButtonInteraction
desc
### Использование
```php
$isButtonInteractionnull
```

## Пример(ы)

```javascript
bot.command({
  name: '$isButtonInteraction',
  code: `
$isButtonInteractionnull`
// Возвращает: ...
})
```